{% extends 'base.twig' %}

{% block content %}
    <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h2 class="h5 mb-0">{{ title }}</h2>
            <a href="/tools/check" class="btn btn-sm btn-secondary">Check Structure</a>
        </div>
        <div class="card-body">
            <pre class="mb-0 small">{{ output|e }}</pre>
        </div>
    </div>
{% endblock %}
